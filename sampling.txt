{% for rm in personal_rooms %}
    {{ rm.room.name }} — {{ rm.can_control }}
{% endfor %}

{% for rm in shared_rooms %}
    {{ rm.room.name }}
{% endfor %}

{% for gr in guest_rooms %}
    {{ gr.room.name }} — expires {{ gr.expires_at }}
    {% if gr.is_currently_accessible() %}✓ accessible{% endif %}
{% endfor %}

{% if admin_overview %}
    {{ admin_overview.total_rooms }} total rooms
{% endif %}